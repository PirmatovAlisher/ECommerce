{
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    },
    "AllowedHosts": "*",
    "RedisCacheSettings": {
        "ConnectionString": "localhost:6379" // Default Redis port
    },
    "JwtSettings": {
        "Key": "HTPwqHwo5qn3kjWeyym1iSgTa0QNefpe", // Must match IdentityService's key
        "Issuer": "ECommerceIdentityService", // Must match IdentityService's issuer
        "Audience": "ECommerceClients" // Must match IdentityService's audience
    }
}
